<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Connect Your Project Manager</title>
    {{ importmap('app') }}
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen bg-slate-900 flex items-center justify-center p-4">
    <div class="w-full max-w-sm">
        <div class="bg-slate-800 rounded-xl shadow-2xl p-6 border border-slate-700">
            <h1 class="text-xl font-semibold text-white text-center mb-2">Connect Your Project Manager</h1>

            {% if google_user_name is defined and google_user_name %}
                <p class="text-slate-400 text-sm text-center mb-6">{{ google_user_email }}</p>
            {% endif %}

            {{ component('ProviderCredentialsForm') }}
        </div>
    </div>
</body>
</html>