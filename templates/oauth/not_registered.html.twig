<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Access Denied - MCP</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        sans: ['Inter', 'system-ui', 'sans-serif'],
                    },
                }
            }
        }
    </script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
</head>
<body class="min-h-screen bg-neutral-950 text-neutral-100 antialiased font-sans">
    <div class="min-h-screen flex items-center justify-center p-6">
        <div class="w-full max-w-md text-center">
            <!-- Logo -->
            <div class="flex items-center justify-center gap-2.5 mb-10">
                <div class="w-10 h-10 bg-white rounded-lg flex items-center justify-center">
                    <svg class="w-6 h-6 text-neutral-900" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M13 10V3L4 14h7v7l9-11h-7z"/>
                    </svg>
                </div>
                <span class="text-xl font-semibold">MCP</span>
            </div>

            <!-- Error Icon -->
            <div class="w-16 h-16 mx-auto mb-6 bg-red-900/20 rounded-full flex items-center justify-center">
                <svg class="w-8 h-8 text-red-400" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/>
                </svg>
            </div>

            <!-- Message -->
            <h1 class="text-2xl font-semibold tracking-tight mb-3">
                Account Not Found
            </h1>
            <p class="text-neutral-400 mb-8">
                The email <span class="text-neutral-200 font-medium">{{ email }}</span> is not registered in any organization.
            </p>

            <div class="bg-neutral-900 border border-neutral-800 rounded-lg p-4 text-sm text-neutral-400">
                <p class="mb-2">To get access:</p>
                <ul class="text-left space-y-1">
                    <li class="flex items-start gap-2">
                        <span class="text-neutral-600">1.</span>
                        <span>Ask your organization admin for an invite link</span>
                    </li>
                    <li class="flex items-start gap-2">
                        <span class="text-neutral-600">2.</span>
                        <span>Open the invite link to join the organization</span>
                    </li>
                    <li class="flex items-start gap-2">
                        <span class="text-neutral-600">3.</span>
                        <span>Come back here and try again</span>
                    </li>
                </ul>
            </div>
        </div>
    </div>
</body>
</html>